
  # Add API Annotations

  This is a code bundle for Add API Annotations. The original project is available at https://www.figma.com/design/u1WL5kmTV9TCruBnJ9hA4X/Add-API-Annotations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  